{
  "name": "tampermonkey-collection",
  "version": "1.0.0",
  "description": "Collection of Tampermonkey scripts",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "generate:extensions": "node scripts/generate-extensions.js",
    "generate:icons": "node scripts/generate-icons.js",
    "prepare": "husky install",
    "generate:all": "npm run build && npm run generate:extensions && npm run generate:icons",
    "postinstall": "npm run generate:all"
  },
  "keywords": ["tampermonkey", "userscripts", "browser-extensions"],
  "author": "Brent Johnson",
  "license": "MIT",
  "devDependencies": {
    "@types/greasemonkey": "^4.0.5",
    "@types/node": "^20.11.19",
    "husky": "^9.0.10",
    "typescript": "^5.3.3",
    "lint-staged": "^15.0.0",
    "prettier": "^3.0.0"
  },
  "dependencies": {
    "canvas": "^2.11.2"
  },
  "optionalDependencies": {
    "canvas": "^2.11.2"
  },
  "resolutions": {
    "canvas": "2.11.2"
  },
  "config": {
    "optional": "false"
  }
}
