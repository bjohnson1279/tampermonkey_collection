## YYYY-MM-DD - Hide keyboard shortcut hints in disabled and transient states
**Learning:** When injecting contextual UI elements with keyboard shortcuts, conditionally rendering the shortcut hint to only appear when the element is actively available prevents visual clutter and user confusion in disabled or transient states.
**Action:** Conditionally render keyboard shortcut hints only when the UI element is interactive.
